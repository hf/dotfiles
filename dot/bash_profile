# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
  . ~/.bashrc
fi

# User specific environment and startup programs

for envfile in $HOME/.env.d/*.env;
do
  source "$envfile"
done
